# v2.1.0 — Foundation Supplement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 vimprove 补齐基础导航命令（gg / G / {N}G / `{` / `}` / `%`）、大写快捷形式（D / C / Y / S）和大小写操作符（`~` / gu / gU / g~），新增 5 节课程，每章末尾加可下载 Cheat Sheet。

**Architecture:** 扩展 `src/core` 纯函数引擎（motions / operators / reducer / types），新增 ContentBlock 类型 `cheat-sheet`，在 `chapter1` 与 `chapter3` 末尾追加课程文件，i18n 同步更新。引擎扩展严格 TDD，每个命令先写失败测试。

**Tech Stack:** TypeScript + Vitest + React 19 + Tailwind 3 + i18next + `html-to-image` (新增依赖, ~30 KB)。

**版本号目标:** `2.0.1` → `2.1.0`

---

## File Structure

### 新建文件
| 路径 | 责任 |
|---|---|
| `src/core/largeMotions.ts` | 新增 motions: `gg` / `G` / `{N}G` / `{` / `}` / `%` 的纯函数实现 |
| `src/core/caseOperators.ts` | `~` 字符级反转、`applyCaseOperatorWithMotion` (gu/gU/g~) |
| `src/core/largeMotions.test.ts` | gg / G / brace / percent 单测 |
| `src/core/caseOperators.test.ts` | `~` / gu / gU / g~ 单测 |
| `src/components/common/CheatSheetBlock.tsx` | 章节命令卡 + 下载为 PNG |
| `src/data/lessons/chapter1/motions-file-bounds.ts` | gg / G / {N}G 课程 |
| `src/data/lessons/chapter1/motions-paragraph.ts` | `{` / `}` 课程 |
| `src/data/lessons/chapter1/motions-bracket-match.ts` | `%` 课程 |
| `src/data/lessons/chapter3/operator-shortcuts.ts` | D / C / Y / S 课程 |
| `src/data/lessons/chapter3/operator-case.ts` | `~` / gu / gU / g~ 课程 |

### 修改文件
| 路径 | 改动 |
|---|---|
| `src/core/types.ts` | 扩展 `Motion` 联合、扩展 `Operator` 联合、新增 ContentBlock `cheat-sheet` |
| `src/core/motions.ts` | `getMotionTarget` 增加 gg/G/brace/percent 分支（委托给 largeMotions） |
| `src/core/operators.ts` | 新增 `applyCaseOperatorWithMotion` / `applyCaseOperatorWithTextObject`；buildRegisterText 等保持不变 |
| `src/core/vimReducer.ts` | 新增 pending `g` 状态，接键 `gg`/`G`/`gu`/`gU`/`g~`/`{`/`}`/`%`/`D`/`C`/`Y`/`S`/`~`；count prefix `{N}G` |
| `src/core/tests/exhaustiveTestCases.ts` | 新增 FeatureId `motion_gg_G`、`motion_brace`、`motion_percent`、`operator_case` |
| `src/components/lesson/LessonView.tsx` | 派发 `cheat-sheet` block |
| `src/data/index.ts` | import + push 5 节新课 |
| `src/i18n/locales/zh/lessons.json` | 5 节新课的中文翻译 |
| `src/i18n/locales/zh-lively/lessons.json` | 5 节新课的活泼中文翻译 |
| `src/i18n/locales/zh/challenge.json` | Cheat Sheet UI 文案 |
| `src/i18n/locales/zh-lively/challenge.json` | Cheat Sheet UI 文案 |
| `package.json` + `src/version.ts` | 版本号 2.1.0 + 新增 `html-to-image` 依赖 |
| `README.md` | CHANGELOG 增加 v2.1.0 条目 |
| `CLAUDE.md` | "已支持的命令" 章节追加新命令清单 |

---

## Task 1: 扩展 types.ts（Motion / Operator union）

**Files:**
- Modify: `src/core/types.ts:10` (Motion union), `src/core/types.ts:8` (Operator union), `src/core/types.ts:155` (ContentBlock union)

- [ ] **Step 1: 修改 Motion / Operator / ContentBlock union**

替换 `src/core/types.ts` 第 8 行：

```typescript
export type Operator = 'd' | 'c' | 'y' | 'gu' | 'gU' | 'g~';
```

替换第 10 行：

```typescript
export type Motion =
  | 'h' | 'j' | 'k' | 'l'
  | 'w' | 'b' | 'e'
  | 'W' | 'B' | 'E'
  | '0' | '$' | '^' | '_'
  | 'gg' | 'G'
  | '{' | '}'
  | '%';
```

在 ContentBlock union（第 155 行起）末尾追加一项：

```typescript
  | { type: 'cheat-sheet'; config: CheatSheetConfig; i18nKey?: string };
```

并在文件中靠近 `Game2048Config` 处追加：

```typescript
export type CheatSheetConfig = {
  chapterId: string;       // 'chapter1' | 'chapter3' | ...
  title?: string;          // 可选覆盖默认 "Chapter X — Cheat Sheet"
  // entries 由组件根据 chapterId 在运行时从 LESSONS 聚合，因此 config 极简
};
```

- [ ] **Step 2: typecheck 通过**

Run: `npx tsc -p tsconfig.app.json --noEmit`
Expected: PASS（若有现存类型错误请单独修复）

- [ ] **Step 3: Commit**

```bash
git add src/core/types.ts
git commit -m "feat(core): extend Motion/Operator/ContentBlock unions for v2.1.0"
```

---

## Task 2: 实现 `gg` / `G` / `{N}G`

**Files:**
- Create: `src/core/largeMotions.ts`, `src/core/largeMotions.test.ts`
- Modify: `src/core/motions.ts` (在 default 分支前加 case)

### TDD 流程

- [ ] **Step 1: 写失败测试 (`src/core/largeMotions.test.ts`)**

```typescript
import { describe, it, expect } from 'vitest';
import { gotoFirstLine, gotoLastLine, gotoLineN } from './largeMotions';
import { clampCursor } from './utils';

const buf = (lines: string[]) => lines;
const cursor = (l: number, c: number) => ({ line: l, col: c });

describe('large motions: gg / G / {N}G', () => {
  it('gg jumps to first non-blank of line 0', () => {
    const buffer = buf(['  alpha', 'beta', '  gamma']);
    const result = gotoFirstLine(buffer, cursor(2, 5));
    expect(result).toEqual({ line: 0, col: 2 });
  });

  it('G with no count jumps to first non-blank of last line', () => {
    const buffer = buf(['alpha', '   beta', '  gamma']);
    const result = gotoLastLine(buffer, cursor(0, 0));
    expect(result).toEqual({ line: 2, col: 2 });
  });

  it('{N}G jumps to first non-blank of line N (1-based)', () => {
    const buffer = buf(['l0', '  l1', '    l2']);
    const result = gotoLineN(buffer, 2);
    expect(result).toEqual({ line: 1, col: 2 });
  });

  it('{N}G clamps to last line when N exceeds buffer length', () => {
    const buffer = buf(['l0', 'l1']);
    const result = gotoLineN(buffer, 99);
    expect(result).toEqual({ line: 1, col: 0 });
  });

  it('gg on empty line lands at col 0', () => {
    const buffer = buf(['', 'beta']);
    const result = gotoFirstLine(buffer, cursor(1, 0));
    expect(result).toEqual({ line: 0, col: 0 });
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 5 个测试失败（cannot find module './largeMotions'）

- [ ] **Step 3: 实现 `largeMotions.ts`**

```typescript
import type { Cursor } from './types';
import { isWhitespace } from './utils';

const firstNonBlankCol = (line: string): number => {
  for (let i = 0; i < line.length; i++) {
    if (!isWhitespace(line[i])) return i;
  }
  return 0;
};

/** `gg` — jump to first non-blank of line 0 */
export const gotoFirstLine = (buffer: string[], _cursor: Cursor): Cursor => {
  if (buffer.length === 0) return { line: 0, col: 0 };
  return { line: 0, col: firstNonBlankCol(buffer[0] ?? '') };
};

/** `G` (no count) — jump to first non-blank of last line */
export const gotoLastLine = (buffer: string[], _cursor: Cursor): Cursor => {
  if (buffer.length === 0) return { line: 0, col: 0 };
  const last = buffer.length - 1;
  return { line: last, col: firstNonBlankCol(buffer[last] ?? '') };
};

/** `{N}G` — jump to line N (1-based), clamped to buffer length */
export const gotoLineN = (buffer: string[], lineN: number): Cursor => {
  if (buffer.length === 0) return { line: 0, col: 0 };
  const target = Math.min(Math.max(1, lineN), buffer.length) - 1;
  return { line: target, col: firstNonBlankCol(buffer[target] ?? '') };
};
```

- [ ] **Step 4: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 5 pass

- [ ] **Step 5: 在 motions.ts 的 getMotionTarget 中加分支**

在 `src/core/motions.ts:480` 的 `default:` 之前插入：

```typescript
    case 'gg':
      return gotoFirstLine(buffer, cursor);

    case 'G': {
      const cnt = (state as VimState & { count?: string }).count;
      if (cnt && cnt.length > 0) return gotoLineN(buffer, parseInt(cnt, 10));
      return gotoLastLine(buffer, cursor);
    }
```

并在文件顶部 import：

```typescript
import { gotoFirstLine, gotoLastLine, gotoLineN } from './largeMotions';
```

> 注：`getMotionTarget` 当前签名 `(state, motion, forOperator)` 已经持有 `state.count`，无需新增参数。

- [ ] **Step 6: Commit**

```bash
git add src/core/largeMotions.ts src/core/largeMotions.test.ts src/core/motions.ts
git commit -m "feat(core): implement gg / G / {N}G motions"
```

---

## Task 3: 在 reducer 接 `g` pending 与 `G` 键

**Files:**
- Modify: `src/core/vimReducer.ts` (在 KEYDOWN 分支适当位置)

- [ ] **Step 1: 在 VimState 增加 `pendingG` 字段**

修改 `src/core/types.ts` 第 37 行的 `VimState`，追加：

```typescript
  pendingG: boolean;  // true after first 'g', awaiting 'g'/'u'/'U'/'~'/'e'/'E'
```

并在 `vimReducer.ts` 的 `INITIAL_VIM_STATE` 中加 `pendingG: false`。

- [ ] **Step 2: 写失败测试**

新建 `src/core/largeMotions-reducer.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { vimReducer, INITIAL_VIM_STATE } from './vimReducer';

const keydown = (state: typeof INITIAL_VIM_STATE, key: string) =>
  vimReducer(state, { type: 'KEYDOWN', payload: { key } });

const seed = (lines: string[], line = 0, col = 0) => ({
  ...INITIAL_VIM_STATE,
  buffer: lines,
  cursor: { line, col }
});

describe('reducer: gg / G', () => {
  it('gg jumps to first non-blank of line 0', () => {
    let s = seed(['  alpha', 'beta', '  gamma'], 2, 5);
    s = keydown(s, 'g');
    expect(s.pendingG).toBe(true);
    s = keydown(s, 'g');
    expect(s.pendingG).toBe(false);
    expect(s.cursor).toEqual({ line: 0, col: 2 });
  });

  it('G with no count jumps to last line', () => {
    let s = seed(['l0', 'l1', '  l2'], 0, 0);
    s = keydown(s, 'G');
    expect(s.cursor).toEqual({ line: 2, col: 2 });
  });

  it('5G jumps to line 5 (1-based)', () => {
    let s = seed(['l0','l1','l2','l3','l4','l5','l6']);
    s = keydown(s, '5');
    s = keydown(s, 'G');
    expect(s.cursor).toEqual({ line: 4, col: 0 });
    expect(s.count).toBe('');
  });

  it('Esc clears g pending', () => {
    let s = seed(['hello']);
    s = keydown(s, 'g');
    expect(s.pendingG).toBe(true);
    s = keydown(s, 'Escape');
    expect(s.pendingG).toBe(false);
  });
});
```

- [ ] **Step 3: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/largeMotions-reducer.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 4 fail

- [ ] **Step 4: 在 reducer KEYDOWN 分支添加 g pending 处理**

在 `vimReducer.ts` 第 1045 行 (`if (key === 'd')`) 之前插入：

```typescript
  // ----- g pending: 'g' awaits 'g'/'u'/'U'/'~'/'e'/'E' -----
  if (state.pendingG) {
    const clearedG = { ...state, pendingG: false };
    if (key === 'g') {
      const target = getMotionTarget(clearedG, 'gg');
      return {
        ...clearedG,
        cursor: target,
        count: '',
        lastCommand: { type: 'move', motion: 'gg' }
      };
    }
    // gu / gU / g~ handled in Task 9
    // Unknown follow-up key: clear pending and re-dispatch
    return clearedG;
  }

  if (key === 'g') {
    return { ...state, pendingG: true };
  }

  if (key === 'G') {
    const target = getMotionTarget(state, 'G');
    return {
      ...state,
      cursor: target,
      count: '',
      lastCommand: { type: 'move', motion: 'G' }
    };
  }
```

并确保 `Escape` 分支（搜索文件中 `key === 'Escape'`）也清 `pendingG`：在 ESC handler 内追加 `pendingG: false`。

- [ ] **Step 5: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/largeMotions-reducer.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 4 pass

- [ ] **Step 6: 全量回归测试**

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅（dot-command 的 5 个预先存在的失败可以接受，对比 master 数量不增加）

- [ ] **Step 7: Commit**

```bash
git add src/core/vimReducer.ts src/core/types.ts src/core/largeMotions-reducer.test.ts
git commit -m "feat(core): hook gg/G/{N}G into reducer with pendingG state"
```

---

## Task 4: 实现 `{` / `}` 段落跳转

**Files:**
- Modify: `src/core/largeMotions.ts`, `src/core/largeMotions.test.ts`, `src/core/motions.ts`, `src/core/vimReducer.ts`

- [ ] **Step 1: 写失败测试（追加到 largeMotions.test.ts）**

```typescript
describe('large motions: { / } (paragraph)', () => {
  // Buffer: 3 blocks separated by blank lines
  const buf3 = [
    'line a1',
    'line a2',
    '',
    'line b1',
    'line b2',
    '',
    'line c1'
  ];

  it('} from middle of first block jumps to first blank line', () => {
    const result = paraNext(buf3, { line: 0, col: 0 });
    expect(result).toEqual({ line: 2, col: 0 });
  });

  it('} from blank line jumps to next blank line', () => {
    const result = paraNext(buf3, { line: 2, col: 0 });
    expect(result).toEqual({ line: 5, col: 0 });
  });

  it('} from last block jumps to last line', () => {
    const result = paraNext(buf3, { line: 6, col: 0 });
    expect(result).toEqual({ line: 6, col: result.col });
  });

  it('{ from middle of last block jumps to previous blank line', () => {
    const result = paraPrev(buf3, { line: 6, col: 0 });
    expect(result).toEqual({ line: 5, col: 0 });
  });

  it('{ from first block jumps to line 0', () => {
    const result = paraPrev(buf3, { line: 0, col: 3 });
    expect(result).toEqual({ line: 0, col: 0 });
  });
});
```

并在测试文件顶部添加 import：

```typescript
import { gotoFirstLine, gotoLastLine, gotoLineN, paraNext, paraPrev } from './largeMotions';
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 5 fail（cannot find paraNext/paraPrev）

- [ ] **Step 3: 实现 paraNext / paraPrev**

在 `src/core/largeMotions.ts` 追加：

```typescript
const isBlank = (line: string): boolean => line.trim().length === 0;

/** `}` — forward to next blank line (or EOF) */
export const paraNext = (buffer: string[], cursor: Cursor): Cursor => {
  for (let i = cursor.line + 1; i < buffer.length; i++) {
    if (isBlank(buffer[i] ?? '')) return { line: i, col: 0 };
  }
  // No blank line ahead → last line, last col
  if (buffer.length === 0) return { line: 0, col: 0 };
  const last = buffer.length - 1;
  const lastLine = buffer[last] ?? '';
  return { line: last, col: Math.max(0, lastLine.length - 1) };
};

/** `{` — backward to previous blank line (or BOF) */
export const paraPrev = (buffer: string[], cursor: Cursor): Cursor => {
  for (let i = cursor.line - 1; i >= 0; i--) {
    if (isBlank(buffer[i] ?? '')) return { line: i, col: 0 };
  }
  return { line: 0, col: 0 };
};
```

- [ ] **Step 4: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: all pass

- [ ] **Step 5: 在 motions.ts 加 case**

在 `getMotionTarget` 中加：

```typescript
    case '{':
      return paraPrev(buffer, cursor);
    case '}':
      return paraNext(buffer, cursor);
```

并 import 之。

- [ ] **Step 6: 在 reducer 加键接收**

在 vimReducer.ts，Task 3 添加的 `if (key === 'G')` 之后追加：

```typescript
  if (key === '{') {
    const target = getMotionTarget(state, '{');
    return { ...state, cursor: target, count: '', lastCommand: { type: 'move', motion: '{' } };
  }
  if (key === '}') {
    const target = getMotionTarget(state, '}');
    return { ...state, cursor: target, count: '', lastCommand: { type: 'move', motion: '}' } };
  }
```

- [ ] **Step 7: 跑全量测试**

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅

- [ ] **Step 8: Commit**

```bash
git add src/core/largeMotions.ts src/core/largeMotions.test.ts src/core/motions.ts src/core/vimReducer.ts
git commit -m "feat(core): implement { / } paragraph motions"
```

---

## Task 5: 实现 `%` 括号匹配（单行）

**Files:**
- Modify: `src/core/largeMotions.ts`, `src/core/largeMotions.test.ts`, `src/core/motions.ts`, `src/core/vimReducer.ts`

> **范围限制**：本 Task 仅实现**当前行内**的括号匹配 `()[]{}`。跨多行的匹配作为 v3.0 增强项。

- [ ] **Step 1: 写失败测试**

追加到 `largeMotions.test.ts`：

```typescript
describe('large motions: % (bracket match on same line)', () => {
  it('matches ( -> ) on same line', () => {
    const result = bracketMatch(['foo(bar)'], { line: 0, col: 3 });
    expect(result).toEqual({ line: 0, col: 7 });
  });

  it('matches ) -> ( on same line', () => {
    const result = bracketMatch(['foo(bar)'], { line: 0, col: 7 });
    expect(result).toEqual({ line: 0, col: 3 });
  });

  it('matches nested brackets', () => {
    const result = bracketMatch(['(a(b)c)'], { line: 0, col: 0 });
    expect(result).toEqual({ line: 0, col: 6 });
  });

  it('returns same cursor if not on a bracket', () => {
    const cursor = { line: 0, col: 1 };
    const result = bracketMatch(['xxxxx'], cursor);
    expect(result).toEqual(cursor);
  });

  it('returns same cursor if unmatched', () => {
    const cursor = { line: 0, col: 0 };
    const result = bracketMatch(['('], cursor);
    expect(result).toEqual(cursor);
  });

  it('matches { } and [ ]', () => {
    expect(bracketMatch(['{a}'], { line: 0, col: 0 })).toEqual({ line: 0, col: 2 });
    expect(bracketMatch(['[a]'], { line: 0, col: 2 })).toEqual({ line: 0, col: 0 });
  });
});
```

更新顶部 import：

```typescript
import { gotoFirstLine, gotoLastLine, gotoLineN, paraNext, paraPrev, bracketMatch } from './largeMotions';
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 6 fail

- [ ] **Step 3: 实现 bracketMatch**

```typescript
const OPEN_TO_CLOSE: Record<string, string> = { '(': ')', '[': ']', '{': '}' };
const CLOSE_TO_OPEN: Record<string, string> = { ')': '(', ']': '[', '}': '{' };

/** `%` — single-line bracket match. Returns unchanged cursor if no match. */
export const bracketMatch = (buffer: string[], cursor: Cursor): Cursor => {
  const line = buffer[cursor.line] ?? '';
  if (line.length === 0 || cursor.col >= line.length) return cursor;
  const ch = line[cursor.col];

  if (OPEN_TO_CLOSE[ch]) {
    const close = OPEN_TO_CLOSE[ch];
    let depth = 1;
    for (let i = cursor.col + 1; i < line.length; i++) {
      if (line[i] === ch) depth++;
      else if (line[i] === close) {
        depth--;
        if (depth === 0) return { line: cursor.line, col: i };
      }
    }
    return cursor;
  }

  if (CLOSE_TO_OPEN[ch]) {
    const open = CLOSE_TO_OPEN[ch];
    let depth = 1;
    for (let i = cursor.col - 1; i >= 0; i--) {
      if (line[i] === ch) depth++;
      else if (line[i] === open) {
        depth--;
        if (depth === 0) return { line: cursor.line, col: i };
      }
    }
    return cursor;
  }

  return cursor;
};
```

- [ ] **Step 4: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/largeMotions.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: all pass

- [ ] **Step 5: 接入 motions.ts + reducer**

motions.ts 增加：

```typescript
    case '%':
      return bracketMatch(buffer, cursor);
```

vimReducer.ts 在 `}` 处理之后追加：

```typescript
  if (key === '%') {
    const target = getMotionTarget(state, '%');
    return { ...state, cursor: target, count: '', lastCommand: { type: 'move', motion: '%' } };
  }
```

- [ ] **Step 6: 全量回归**

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅

- [ ] **Step 7: Commit**

```bash
git add src/core/largeMotions.ts src/core/largeMotions.test.ts src/core/motions.ts src/core/vimReducer.ts
git commit -m "feat(core): implement % bracket match (single line)"
```

---

## Task 6: 大写快捷 `D` / `C` / `Y` / `S`

**Files:**
- Modify: `src/core/vimReducer.ts`, `src/core/operators.test.ts`

> **设计**：`D` = `d$`、`C` = `c$`、`Y` = `yy`、`S` = `cc`。直接在 KEYDOWN 分支映射到现有路径，不新增 motion。

- [ ] **Step 1: 写失败测试**

在 `src/core/operators.test.ts` 末尾追加：

```typescript
import { vimReducer, INITIAL_VIM_STATE } from './vimReducer';
const kd = (s: typeof INITIAL_VIM_STATE, key: string) =>
  vimReducer(s, { type: 'KEYDOWN', payload: { key } });
const seed = (lines: string[], line = 0, col = 0) => ({
  ...INITIAL_VIM_STATE, buffer: lines, cursor: { line, col }
});

describe('uppercase shortcuts: D / C / Y / S', () => {
  it('D deletes from cursor to end of line', () => {
    let s = seed(['hello world'], 0, 6);
    s = kd(s, 'D');
    expect(s.buffer).toEqual(['hello ']);
    expect(s.register).toBe('world');
  });

  it('C deletes to EOL and enters insert', () => {
    let s = seed(['hello world'], 0, 6);
    s = kd(s, 'C');
    expect(s.buffer).toEqual(['hello ']);
    expect(s.mode).toBe('insert');
  });

  it('Y yanks current line (linewise)', () => {
    let s = seed(['alpha', 'beta'], 0, 2);
    s = kd(s, 'Y');
    expect(s.register).toBe('alpha');
  });

  it('S deletes line content and enters insert at col 0', () => {
    let s = seed(['    indented'], 0, 6);
    s = kd(s, 'S');
    expect(s.buffer).toEqual(['']);
    expect(s.mode).toBe('insert');
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/operators.test.ts -t "uppercase shortcuts" 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 4 fail

- [ ] **Step 3: 在 reducer 加键映射**

在 vimReducer.ts 已有 `if (key === 'X')` 块之后追加：

```typescript
  // ----- Uppercase shortcuts: D = d$, C = c$, Y = yy, S = cc -----
  if (key === 'D') {
    return vimReducer(
      vimReducer(state, { type: 'KEYDOWN', payload: { key: 'd' } }),
      { type: 'KEYDOWN', payload: { key: '$' } }
    );
  }
  if (key === 'C') {
    return vimReducer(
      vimReducer(state, { type: 'KEYDOWN', payload: { key: 'c' } }),
      { type: 'KEYDOWN', payload: { key: '$' } }
    );
  }
  if (key === 'Y') {
    return vimReducer(
      vimReducer(state, { type: 'KEYDOWN', payload: { key: 'y' } }),
      { type: 'KEYDOWN', payload: { key: 'y' } }
    );
  }
  if (key === 'S') {
    return vimReducer(
      vimReducer(state, { type: 'KEYDOWN', payload: { key: 'c' } }),
      { type: 'KEYDOWN', payload: { key: 'c' } }
    );
  }
```

- [ ] **Step 4: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/operators.test.ts -t "uppercase shortcuts" 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 4 pass

- [ ] **Step 5: 全量回归**

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅

- [ ] **Step 6: Commit**

```bash
git add src/core/vimReducer.ts src/core/operators.test.ts
git commit -m "feat(core): add D / C / Y / S uppercase shortcuts"
```

---

## Task 7: `~` 单字符大小写反转

**Files:**
- Create: `src/core/caseOperators.ts`, `src/core/caseOperators.test.ts`
- Modify: `src/core/vimReducer.ts`

- [ ] **Step 1: 写失败测试**

`src/core/caseOperators.test.ts`：

```typescript
import { describe, it, expect } from 'vitest';
import { toggleCharCase, applyCaseRange } from './caseOperators';

describe('caseOperators: toggleCharCase', () => {
  it('lowercase -> uppercase', () => {
    expect(toggleCharCase('a')).toBe('A');
  });
  it('uppercase -> lowercase', () => {
    expect(toggleCharCase('Z')).toBe('z');
  });
  it('non-letter unchanged', () => {
    expect(toggleCharCase('1')).toBe('1');
    expect(toggleCharCase('!')).toBe('!');
  });
});

describe('caseOperators: applyCaseRange', () => {
  it('toggle range upper/lower', () => {
    const result = applyCaseRange(['Hello'], { line: 0, col: 0 }, { line: 0, col: 4 }, 'toggle');
    expect(result).toEqual(['hELLO']);
  });
  it('to upper', () => {
    const result = applyCaseRange(['hello'], { line: 0, col: 0 }, { line: 0, col: 4 }, 'upper');
    expect(result).toEqual(['HELLO']);
  });
  it('to lower', () => {
    const result = applyCaseRange(['HELLO'], { line: 0, col: 0 }, { line: 0, col: 4 }, 'lower');
    expect(result).toEqual(['hello']);
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/caseOperators.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 6 fail

- [ ] **Step 3: 实现 caseOperators.ts**

```typescript
import type { Cursor } from './types';

export type CaseMode = 'toggle' | 'upper' | 'lower';

export const toggleCharCase = (ch: string): string => {
  if (ch >= 'a' && ch <= 'z') return ch.toUpperCase();
  if (ch >= 'A' && ch <= 'Z') return ch.toLowerCase();
  return ch;
};

const transformChar = (ch: string, mode: CaseMode): string => {
  if (mode === 'upper') return ch.toUpperCase();
  if (mode === 'lower') return ch.toLowerCase();
  return toggleCharCase(ch);
};

/** Apply case transform to buffer range [start..end] inclusive. */
export const applyCaseRange = (
  buffer: string[],
  start: Cursor,
  end: Cursor,
  mode: CaseMode
): string[] => {
  const result = [...buffer];
  if (start.line === end.line) {
    const line = result[start.line] ?? '';
    const before = line.slice(0, start.col);
    const target = line.slice(start.col, end.col + 1);
    const after = line.slice(end.col + 1);
    const transformed = target.split('').map(c => transformChar(c, mode)).join('');
    result[start.line] = before + transformed + after;
    return result;
  }
  // Multi-line: transform from start.col on start.line, full middle lines, then 0..end.col on end.line
  const startLine = result[start.line] ?? '';
  result[start.line] = startLine.slice(0, start.col) +
    startLine.slice(start.col).split('').map(c => transformChar(c, mode)).join('');
  for (let i = start.line + 1; i < end.line; i++) {
    result[i] = (result[i] ?? '').split('').map(c => transformChar(c, mode)).join('');
  }
  const endLine = result[end.line] ?? '';
  result[end.line] = endLine.slice(0, end.col + 1).split('').map(c => transformChar(c, mode)).join('') +
    endLine.slice(end.col + 1);
  return result;
};
```

- [ ] **Step 4: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/caseOperators.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: all pass

- [ ] **Step 5: 在 reducer 接 `~` 键**

在 vimReducer.ts 的 `'X'` 处理之后插入（在大写快捷之前）：

```typescript
  if (key === '~') {
    const lineText = buffer[cursor.line] ?? '';
    if (cursor.col >= lineText.length) return state;
    const count = getCount(state);
    const endCol = Math.min(lineText.length - 1, cursor.col + count - 1);
    const stateWithHistory = pushHistory(state);
    const stateWithRecording = startRecording(stateWithHistory, key, ctrlKey || false);
    const newBuffer = applyCaseRange(buffer, cursor, { line: cursor.line, col: endCol }, 'toggle');
    const newCol = Math.min(endCol + 1, newBuffer[cursor.line].length - 1);
    const finished = finishRecording({
      ...stateWithRecording,
      buffer: newBuffer,
      cursor: { ...cursor, col: Math.max(0, newCol) }
    });
    return { ...finished, count: '', lastCommand: { type: 'delete-char' /* reuse */ } };
  }
```

并在文件顶部 import `applyCaseRange`。

- [ ] **Step 6: 写 reducer 测试 + 跑全量**

在 `caseOperators.test.ts` 增加 reducer 集成测试：

```typescript
import { vimReducer, INITIAL_VIM_STATE } from './vimReducer';

describe('reducer: ~', () => {
  it('toggles char under cursor and moves right', () => {
    let s = { ...INITIAL_VIM_STATE, buffer: ['hello'], cursor: { line: 0, col: 0 } };
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '~' } });
    expect(s.buffer).toEqual(['Hello']);
    expect(s.cursor).toEqual({ line: 0, col: 1 });
  });

  it('3~ toggles 3 chars', () => {
    let s = { ...INITIAL_VIM_STATE, buffer: ['hello'], cursor: { line: 0, col: 0 } };
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '3' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '~' } });
    expect(s.buffer).toEqual(['HELlo']);
  });
});
```

Run: `npx vitest run --pool=threads src/core/caseOperators.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: all pass

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅

- [ ] **Step 7: Commit**

```bash
git add src/core/caseOperators.ts src/core/caseOperators.test.ts src/core/vimReducer.ts
git commit -m "feat(core): implement ~ case toggle"
```

---

## Task 8: `gu{motion}` / `gU{motion}` / `g~{motion}` operator

**Files:**
- Modify: `src/core/caseOperators.ts`, `src/core/caseOperators.test.ts`, `src/core/vimReducer.ts`

- [ ] **Step 1: 写失败测试（追加）**

```typescript
describe('reducer: gu / gU / g~', () => {
  it('guw lowercases the next word', () => {
    let s = { ...INITIAL_VIM_STATE, buffer: ['HELLO WORLD'], cursor: { line: 0, col: 0 } };
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'g' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'u' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'w' } });
    expect(s.buffer).toEqual(['hello WORLD']);
  });

  it('gU$ uppercases to end of line', () => {
    let s = { ...INITIAL_VIM_STATE, buffer: ['abc def'], cursor: { line: 0, col: 4 } };
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'g' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'U' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '$' } });
    expect(s.buffer).toEqual(['abc DEF']);
  });

  it('g~~ toggles case of current line', () => {
    let s = { ...INITIAL_VIM_STATE, buffer: ['Hello World'], cursor: { line: 0, col: 0 } };
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: 'g' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '~' } });
    s = vimReducer(s, { type: 'KEYDOWN', payload: { key: '~' } });
    expect(s.buffer).toEqual(['hELLO wORLD']);
  });
});
```

- [ ] **Step 2: 跑测试验证失败**

Run: `npx vitest run --pool=threads src/core/caseOperators.test.ts -t "gu" 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: 3 fail

- [ ] **Step 3: 在 g pending 分支扩展**

修改 Task 3 添加的 `if (state.pendingG)` 块：

```typescript
  if (state.pendingG) {
    const clearedG = { ...state, pendingG: false };
    if (key === 'g') {
      const target = getMotionTarget(clearedG, 'gg');
      return { ...clearedG, cursor: target, count: '', lastCommand: { type: 'move', motion: 'gg' } };
    }
    if (key === 'u' || key === 'U' || key === '~') {
      const op = (key === 'u' ? 'gu' : key === 'U' ? 'gU' : 'g~') as 'gu' | 'gU' | 'g~';
      return { ...clearedG, pendingOperator: op };
    }
    return clearedG;
  }
```

- [ ] **Step 4: 处理 `gu` / `gU` / `g~` 在 `pendingOperator` 阶段下的 motion 完成**

在 vimReducer.ts 中找 `state.pendingOperator === 'd'` 等处理逻辑，扩展到 case operators。或者更干净的做法：在 `applyOperatorWithMotion` 调用处增加分支。

简化方案：当 `pendingOperator` 是 case op，且接收 motion / linewise repeat（如 `gu` + `gu` → 整行）：

新增辅助函数（在 vimReducer.ts 或 operators.ts）：

```typescript
import { applyCaseRange, type CaseMode } from './caseOperators';

const opToCaseMode = (op: 'gu' | 'gU' | 'g~'): CaseMode =>
  op === 'gu' ? 'lower' : op === 'gU' ? 'upper' : 'toggle';

const applyCaseOperatorWithMotion = (state: VimState, motion: Motion, op: 'gu' | 'gU' | 'g~'): VimState => {
  const targetCursor = getMotionTarget(state, motion, true);
  const startCursor = state.cursor;
  const [start, end] = sortCursors(startCursor, targetCursor);
  // For exclusive motions like w, end should be one position before targetCursor (handled in operators.ts; reuse if possible)
  const endInclusive = motion === '$' ? end : { line: end.line, col: Math.max(0, end.col - 1) };
  const newBuffer = applyCaseRange(state.buffer, start, endInclusive, opToCaseMode(op));
  return pushHistory({ ...state, buffer: newBuffer, cursor: start, pendingOperator: null, count: '' });
};

const applyCaseOperatorLinewise = (state: VimState, op: 'gu' | 'gU' | 'g~'): VimState => {
  const lineText = state.buffer[state.cursor.line] ?? '';
  if (lineText.length === 0) return { ...state, pendingOperator: null };
  const newBuffer = applyCaseRange(state.buffer, { line: state.cursor.line, col: 0 }, { line: state.cursor.line, col: lineText.length - 1 }, opToCaseMode(op));
  return pushHistory({ ...state, buffer: newBuffer, pendingOperator: null, count: '' });
};
```

> 注：`sortCursors` 若未导出，需在 operators.ts 中加 `export`，或在 caseOperators.ts 中自实现。

在 KEYDOWN 处理 `pendingOperator` 时：

```typescript
  if (state.pendingOperator === 'gu' || state.pendingOperator === 'gU' || state.pendingOperator === 'g~') {
    const op = state.pendingOperator as 'gu' | 'gU' | 'g~';
    // Linewise: same key as operator (`guu`, `gUU`, `g~~`)
    if ((op === 'gu' && key === 'u') || (op === 'gU' && key === 'U') || (op === 'g~' && key === '~')) {
      return applyCaseOperatorLinewise(state, op);
    }
    // Single-char motions
    if (['h','j','k','l','w','b','e','W','B','E','0','$','^','_','{','}','%'].includes(key)) {
      return applyCaseOperatorWithMotion(state, key as Motion, op);
    }
    // Unknown follow-up → cancel
    return { ...state, pendingOperator: null, count: '' };
  }
```

- [ ] **Step 5: 跑测试验证通过**

Run: `npx vitest run --pool=threads src/core/caseOperators.test.ts 2>&1 | grep -EA5 "Failed Tests|Test Files"`
Expected: all pass

- [ ] **Step 6: 全量回归**

Run: `bash utils/vitest-quickcheck.sh`
Expected: ok ✅

- [ ] **Step 7: Commit**

```bash
git add src/core/caseOperators.ts src/core/caseOperators.test.ts src/core/vimReducer.ts
git commit -m "feat(core): implement gu / gU / g~ case operators"
```

---

## Task 9: 新增 `cheat-sheet` ContentBlock 与组件

**Files:**
- Create: `src/components/common/CheatSheetBlock.tsx`
- Modify: `src/components/lesson/LessonView.tsx`, `package.json`

- [ ] **Step 1: 安装依赖**

```bash
npm install html-to-image@1.11.13
```

验证 `package.json` 出现 `"html-to-image"` 条目。

- [ ] **Step 2: 写 CheatSheetBlock**

`src/components/common/CheatSheetBlock.tsx`：

```typescript
import { useRef, useState } from 'react';
import { toPng } from 'html-to-image';
import { CATEGORIES } from '@/data/categories';
import { LESSONS } from '@/data';
import type { CheatSheetConfig, KeyItem } from '@/core/types';
import { useTranslationSafe } from '@/hooks/useI18n';

type Props = { config: CheatSheetConfig };

const collectKeysForChapter = (chapterId: string): KeyItem[] => {
  const seen = new Set<string>();
  const out: KeyItem[] = [];
  for (const lesson of LESSONS) {
    if (lesson.categoryId !== chapterId) continue;
    for (const block of lesson.contentBlocks) {
      if (block.type !== 'key-list') continue;
      for (const k of block.keys) {
        const id = k.chars.join('+');
        if (!seen.has(id)) {
          seen.add(id);
          out.push(k);
        }
      }
    }
  }
  return out;
};

export const CheatSheetBlock = ({ config }: Props) => {
  const { t } = useTranslationSafe('challenge');
  const ref = useRef<HTMLDivElement>(null);
  const [busy, setBusy] = useState(false);

  const chapter = CATEGORIES.find(c => c.id === config.chapterId);
  const keys = collectKeysForChapter(config.chapterId);
  const title = config.title ?? `${chapter?.title ?? config.chapterId} — Cheat Sheet`;

  const download = async () => {
    if (!ref.current) return;
    setBusy(true);
    try {
      const dataUrl = await toPng(ref.current, { pixelRatio: 2, backgroundColor: '#0c0a09' });
      const link = document.createElement('a');
      link.download = `${config.chapterId}-cheat-sheet.png`;
      link.href = dataUrl;
      link.click();
    } finally {
      setBusy(false);
    }
  };

  return (
    <div className="my-12">
      <div ref={ref} className="rounded-xl border border-border bg-surface p-6">
        <h3 className="text-xl font-semibold text-foreground-strong mb-4">{title}</h3>
        <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
          {keys.map((k, idx) => (
            <div key={idx} className="flex items-start gap-3">
              <div className="flex gap-1">
                {k.chars.map((c, i) => (
                  <kbd key={i} className="px-2 py-1 bg-stone-800 text-stone-100 rounded text-sm font-mono">{c}</kbd>
                ))}
              </div>
              <p className="text-sm text-foreground-subtle flex-1">{k.desc}</p>
            </div>
          ))}
        </div>
      </div>
      <button
        onClick={download}
        disabled={busy}
        className="mt-3 px-4 py-2 bg-primary text-primary-foreground rounded text-sm disabled:opacity-50"
      >
        {busy ? t('cheatSheet.exporting', 'Exporting…') : t('cheatSheet.download', 'Download as PNG')}
      </button>
    </div>
  );
};
```

- [ ] **Step 3: 在 LessonView 派发**

`src/components/lesson/LessonView.tsx` 顶部 import：

```typescript
import { CheatSheetBlock } from '@/components/common/CheatSheetBlock';
```

在 `contentBlocks.map` 内（`game-2048` 分支之后）追加：

```typescript
if (block.type === 'cheat-sheet') {
  return <CheatSheetBlock key={reactKey} config={block.config} />;
}
```

- [ ] **Step 4: 手测**

Run: `npm run dev`
打开任一节末尾插入了 `cheat-sheet` block 的课程（Task 10 之后），点 "Download as PNG"，验证下载文件可打开。

> 本 step 在 Task 10 完成之后再做手测。

- [ ] **Step 5: 跑 build**

Run: `npm run build 2>&1 | tail -10`
Expected: build PASS；产物 < 850 KB（html-to-image ~30 KB）

- [ ] **Step 6: Commit**

```bash
git add src/components/common/CheatSheetBlock.tsx src/components/lesson/LessonView.tsx package.json package-lock.json
git commit -m "feat(ui): add CheatSheetBlock with PNG export"
```

---

## Task 10: 新增课程 `motions-file-bounds`

**Files:**
- Create: `src/data/lessons/chapter1/motions-file-bounds.ts`
- Modify: `src/data/index.ts`

- [ ] **Step 1: 写课程文件**

`src/data/lessons/chapter1/motions-file-bounds.ts`：

```typescript
import type { Lesson } from '@/core/types';

export const motionsFileBounds: Lesson = {
  slug: 'motions-file-bounds',
  title: 'File Bounds: gg, G, {N}G',
  categoryId: 'chapter1',
  shortDescription: 'Jump to the top, bottom, or any line by number.',
  contentBlocks: [
    {
      type: 'markdown',
      content: `## Jump anywhere in the file

So far you moved one line at a time with **j** / **k**. For long files, that's too slow.

- **gg** → jump to the **first line** (and first non-blank column).
- **G** → jump to the **last line**.
- **{N}G** → jump to **line N** (1-based). Example: \`5G\` lands on line 5.

These three keys are the most reused motions in real editing. Memorize them.`
    },
    {
      type: 'markdown',
      content: `## Example: gg / G / 5G

Watch the cursor jump from the middle of the file to line 0, then last line, then line 5.`
    },
    {
      type: 'run-example',
      config: {
        initialBuffer: [
          '// file: example.ts',
          'function alpha() {',
          '  return 1;',
          '}',
          '',
          'function beta() {',
          '  return 2;',
          '}',
          '',
          'export { alpha, beta };'
        ],
        initialCursor: { line: 5, col: 0 },
        autoPlaySpeed: 900,
        tracks: [{ label: 'gg / G / 5G', keys: [] }],
        steps: [
          { key: 'g', description: 'g: first half of gg — waits for the next g.', cursorIndex: 0 },
          { key: 'g', description: 'g: completes gg, jumps to line 0.', cursorIndex: 0 },
          { key: 'G', description: 'G: jump to the last line.', cursorIndex: 0 },
          { key: '5', description: '5: count prefix for the next motion.', cursorIndex: 0 },
          { key: 'G', description: '5G: jump to line 5 (1-based).', cursorIndex: 0 }
        ]
      }
    },
    {
      type: 'key-list',
      keys: [
        { chars: ['g', 'g'], desc: 'Go to first line (top of buffer)' },
        { chars: ['G'], desc: 'Go to last line (bottom of buffer)' },
        { chars: ['{N}', 'G'], desc: 'Go to line N (e.g., 5G)' }
      ]
    },
    {
      type: 'challenge',
      config: {
        initialBuffer: [
          'line 1',
          'line 2',
          'line 3',
          'line 4',
          'line 5',
          'line 6',
          'line 7'
        ],
        initialCursor: { line: 3, col: 0 },
        goalsRequired: 3,
        enabledCommands: ['g', 'G', 'h', 'j', 'k', 'l', '0', '$', '1', '2', '3', '4', '5', '6', '7'],
        goals: [
          {
            id: 'top',
            type: 'move',
            description: 'Use gg to jump to line 1.',
            validator: (_p, next) => next.cursor.line === 0
          },
          {
            id: 'bottom',
            type: 'move',
            description: 'Use G to jump to the last line.',
            validator: (_p, next) => next.cursor.line === 6
          },
          {
            id: 'line5',
            type: 'move',
            description: 'Use 5G to jump to line 5 (it is the line "line 5").',
            validator: (_p, next, last) =>
              next.cursor.line === 4 && last?.motion === 'G'
          }
        ]
      }
    }
  ]
};
```

- [ ] **Step 2: 注册到 LESSONS**

修改 `src/data/index.ts`：

在 `// Chapter 1: Modes & Basic Movement` 区块的 imports 末尾添加：

```typescript
import { motionsFileBounds } from './lessons/chapter1/motions-file-bounds';
```

在 `LESSONS` 数组中，把 `motionsFileBounds` 插入到 `motionsLineBounds` 之后、`modesMovementMiniReview` 之前：

```typescript
  motionsLineBounds,
  motionsFileBounds,        // ← 新增
  modesMovementMiniReview,
```

- [ ] **Step 3: 手测**

```bash
npm run dev
# 浏览器访问 http://localhost:3000，切到 motions-file-bounds，跑完 challenge
```

- [ ] **Step 4: typecheck + build**

Run: `npx tsc -p tsconfig.app.json --noEmit && npm run build 2>&1 | tail -5`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add src/data/lessons/chapter1/motions-file-bounds.ts src/data/index.ts
git commit -m "feat(content): add motions-file-bounds lesson (gg / G / {N}G)"
```

---

## Task 11: 新增课程 `motions-paragraph`

**Files:**
- Create: `src/data/lessons/chapter1/motions-paragraph.ts`
- Modify: `src/data/index.ts`

> **结构与 Task 10 完全一致**，仅以下不同：
>
> - slug `motions-paragraph`，title `Paragraphs: { and }`
> - run-example buffer：3 段被空行分隔的代码块
> - key-list：`{` / `}`
> - challenge：目标"跳到下一段的第一行"、"跳到上一段的第一行"
> - 注册位置：紧跟在 `motionsFileBounds` 之后

- [ ] **Step 1: 写课程文件**

```typescript
import type { Lesson } from '@/core/types';

export const motionsParagraph: Lesson = {
  slug: 'motions-paragraph',
  title: 'Paragraphs: { and }',
  categoryId: 'chapter1',
  shortDescription: 'Jump between paragraphs (blank-line separated blocks).',
  contentBlocks: [
    {
      type: 'markdown',
      content: `## What counts as a paragraph?

A **paragraph** is any block of text separated from others by a blank line.
Code naturally forms paragraphs: each function, each import block, each \`if\` arm.

- **\`}\`** → jump **forward** to the next blank line (or end of file).
- **\`{\`** → jump **backward** to the previous blank line (or start of file).

This is the fastest way to navigate by "logical block" without counting lines.`
    },
    {
      type: 'run-example',
      config: {
        initialBuffer: [
          'import x',
          'import y',
          '',
          'function a() {',
          '  return 1;',
          '}',
          '',
          'function b() {',
          '  return 2;',
          '}'
        ],
        initialCursor: { line: 0, col: 0 },
        autoPlaySpeed: 850,
        tracks: [{ label: '{ / }', keys: [] }],
        steps: [
          { key: '}', description: '}: jump forward to the first blank line.', cursorIndex: 0 },
          { key: '}', description: '}: jump to the next blank line.', cursorIndex: 0 },
          { key: '{', description: '{: jump backward to previous blank line.', cursorIndex: 0 },
          { key: '{', description: '{: jump back to start of file.', cursorIndex: 0 }
        ]
      }
    },
    {
      type: 'key-list',
      keys: [
        { chars: ['}'], desc: 'Jump forward to the next blank line' },
        { chars: ['{'], desc: 'Jump backward to the previous blank line' }
      ]
    },
    {
      type: 'challenge',
      config: {
        initialBuffer: [
          'paragraph A line 1',
          'paragraph A line 2',
          '',
          'paragraph B line 1',
          'paragraph B line 2',
          '',
          'paragraph C line 1'
        ],
        initialCursor: { line: 0, col: 0 },
        goalsRequired: 2,
        enabledCommands: ['{', '}', 'h', 'j', 'k', 'l'],
        goals: [
          {
            id: 'next-para',
            type: 'move',
            description: 'Use } to jump to the blank line after paragraph A.',
            validator: (_p, next) => next.cursor.line === 2
          },
          {
            id: 'last-para',
            type: 'move',
            description: 'Continue with } until you reach paragraph C.',
            validator: (_p, next) => next.cursor.line === 6
          }
        ]
      }
    }
  ]
};
```

- [ ] **Step 2: 注册 + commit**

`src/data/index.ts`：

```typescript
import { motionsParagraph } from './lessons/chapter1/motions-paragraph';
// 在数组中:
  motionsFileBounds,
  motionsParagraph,        // ← 新增
  modesMovementMiniReview,
```

```bash
git add src/data/lessons/chapter1/motions-paragraph.ts src/data/index.ts
git commit -m "feat(content): add motions-paragraph lesson ({ / })"
```

---

## Task 12: 新增课程 `motions-bracket-match`

**Files:**
- Create: `src/data/lessons/chapter1/motions-bracket-match.ts`
- Modify: `src/data/index.ts`

- [ ] **Step 1: 写课程文件**

```typescript
import type { Lesson } from '@/core/types';

export const motionsBracketMatch: Lesson = {
  slug: 'motions-bracket-match',
  title: 'Bracket Match: %',
  categoryId: 'chapter1',
  shortDescription: 'Jump between matching ( ), [ ], { } pairs.',
  contentBlocks: [
    {
      type: 'markdown',
      content: `## Match the bracket

Place the cursor on any **\`(\`**, **\`)\`**, **\`[\`**, **\`]\`**, **\`{\`** or **\`}\`** and press **%**.
Vim jumps to its matching pair.

This is essential for navigating function calls, array literals, and code blocks.

> Note: this mini editor matches brackets **on the same line only**. Real Vim handles multi-line as well.`
    },
    {
      type: 'run-example',
      config: {
        initialBuffer: ['function add(a, b) { return a + b; }'],
        initialCursor: { line: 0, col: 12 },
        autoPlaySpeed: 900,
        tracks: [{ label: '% match', keys: [] }],
        steps: [
          { key: '%', description: '%: from ( jump to ).', cursorIndex: 0 },
          { key: '%', description: '%: from ) jump back to (.', cursorIndex: 0 }
        ]
      }
    },
    {
      type: 'key-list',
      keys: [
        { chars: ['%'], desc: 'Jump to the matching bracket' }
      ]
    },
    {
      type: 'challenge',
      config: {
        initialBuffer: ['const tuple = [1, [2, 3], 4];'],
        initialCursor: { line: 0, col: 14 },
        goalsRequired: 2,
        enabledCommands: ['%', 'h', 'l', 'j', 'k'],
        goals: [
          {
            id: 'outer-close',
            type: 'move',
            description: 'Press % to jump to the matching ] of the outer array.',
            validator: (_p, next) => next.cursor.col === 27
          },
          {
            id: 'inner-pair',
            type: 'move',
            description: 'Move to the inner [ at col 18 and press % to jump to its ].',
            validator: (_p, next) => next.cursor.col === 22
          }
        ]
      }
    }
  ]
};
```

- [ ] **Step 2: 注册 + commit**

```typescript
import { motionsBracketMatch } from './lessons/chapter1/motions-bracket-match';
//
  motionsParagraph,
  motionsBracketMatch,        // ← 新增
  modesMovementMiniReview,
```

```bash
git add src/data/lessons/chapter1/motions-bracket-match.ts src/data/index.ts
git commit -m "feat(content): add motions-bracket-match lesson (%)"
```

---

## Task 13: 新增课程 `operator-shortcuts`

**Files:**
- Create: `src/data/lessons/chapter3/operator-shortcuts.ts`
- Modify: `src/data/index.ts`

> 位置：Chapter 3 末尾，**插入到 `operatorsMiniReview` 之后、`game2048` 之前**。

- [ ] **Step 1: 写课程文件**

```typescript
import type { Lesson } from '@/core/types';

export const operatorShortcuts: Lesson = {
  slug: 'operator-shortcuts',
  title: 'Uppercase Shortcuts: D, C, Y, S',
  categoryId: 'chapter3',
  shortDescription: 'One-key shortcuts for the four most common operator + motion combos.',
  contentBlocks: [
    {
      type: 'markdown',
      content: `## Four "save one keystroke" shortcuts

Some operator + motion combos are so common that Vim gives them a single uppercase key:

- **D** ≡ **d$** — delete from cursor to end of line.
- **C** ≡ **c$** — change from cursor to end of line.
- **Y** ≡ **yy** — yank (copy) the entire line.
- **S** ≡ **cc** — substitute the whole line (delete and enter insert).

Once you internalize these, your daily editing becomes noticeably snappier.`
    },
    {
      type: 'key-list',
      keys: [
        { chars: ['D'], desc: 'Delete from cursor to end of line (= d$)' },
        { chars: ['C'], desc: 'Change from cursor to end of line (= c$)' },
        { chars: ['Y'], desc: 'Yank the current line (= yy)' },
        { chars: ['S'], desc: 'Substitute the whole line (= cc)' }
      ]
    },
    {
      type: 'challenge',
      config: {
        initialBuffer: [
          'console.log("debug");  // remove this comment',
          'const value = 42;'
        ],
        initialCursor: { line: 0, col: 23 },
        goalsRequired: 2,
        enabledCommands: ['D', 'C', 'Y', 'S', 'h', 'j', 'k', 'l', 'Escape'],
        goals: [
          {
            id: 'use-D',
            type: 'delete',
            description: 'Use D to remove the inline comment (and trailing spaces) on line 1.',
            validator: (_p, next) => next.buffer[0] === 'console.log("debug");  '
              || next.buffer[0] === 'console.log("debug");'
          },
          {
            id: 'use-Y',
            type: 'custom',
            description: 'Move to line 2 and use Y to yank the whole line into the register.',
            validator: (_p, next) => next.register === 'const value = 42;'
          }
        ]
      }
    }
  ]
};
```

- [ ] **Step 2: 注册 + commit**

```typescript
import { operatorShortcuts } from './lessons/chapter3/operator-shortcuts';
//
  operatorsMiniReview,
  operatorShortcuts,       // ← 新增
  game2048,
```

```bash
git add src/data/lessons/chapter3/operator-shortcuts.ts src/data/index.ts
git commit -m "feat(content): add operator-shortcuts lesson (D / C / Y / S)"
```

---

## Task 14: 新增课程 `operator-case`

**Files:**
- Create: `src/data/lessons/chapter3/operator-case.ts`
- Modify: `src/data/index.ts`

- [ ] **Step 1: 写课程文件**

```typescript
import type { Lesson } from '@/core/types';

export const operatorCase: Lesson = {
  slug: 'operator-case',
  title: 'Case Operators: ~, gu, gU, g~',
  categoryId: 'chapter3',
  shortDescription: 'Toggle / lower / upper case on a char, motion, or whole line.',
  contentBlocks: [
    {
      type: 'markdown',
      content: `## Case is also an operator

You can flip case of:

- A **single char** under the cursor with **~** (just like x / r operate on one char).
- A **range** via **operator + motion**:
  - **gu{motion}** → make range **lower** case
  - **gU{motion}** → make range **UPPER** case
  - **g~{motion}** → **toggle** case
- A **whole line** with doubled key:
  - **guu** / **gUU** / **g~~**

Example: \`gUw\` on \`hello\` → \`HELLO\`. \`gugu\` on a line → all lowercase.`
    },
    {
      type: 'key-list',
      keys: [
        { chars: ['~'], desc: 'Toggle case of the char under cursor and move right' },
        { chars: ['g', 'u', '{motion}'], desc: 'Lowercase over motion (guw / gu$ / gugu)' },
        { chars: ['g', 'U', '{motion}'], desc: 'Uppercase over motion (gUw / gU$ / gUU)' },
        { chars: ['g', '~', '{motion}'], desc: 'Toggle case over motion (g~w / g~~)' }
      ]
    },
    {
      type: 'challenge',
      config: {
        initialBuffer: [
          'const greeting = "hello world";'
        ],
        initialCursor: { line: 0, col: 18 },
        goalsRequired: 2,
        enabledCommands: ['~', 'g', 'u', 'U', 'h', 'l', 'w', 'b', 'e', '$', '0', 'Escape'],
        goals: [
          {
            id: 'upper-hello',
            type: 'change',
            description: 'Use gUw on the word "hello" to make it HELLO.',
            validator: (_p, next) => next.buffer[0].includes('HELLO world')
          },
          {
            id: 'toggle-w',
            type: 'change',
            description: 'Move to "world" (col 24) and use g~w to toggle its case.',
            validator: (_p, next) => next.buffer[0].includes('HELLO WORLD') || next.buffer[0].includes('hello WORLD')
          }
        ]
      }
    }
  ]
};
```

- [ ] **Step 2: 注册 + commit**

```typescript
import { operatorCase } from './lessons/chapter3/operator-case';
//
  operatorShortcuts,
  operatorCase,           // ← 新增
  game2048,
```

```bash
git add src/data/lessons/chapter3/operator-case.ts src/data/index.ts
git commit -m "feat(content): add operator-case lesson (~ / gu / gU / g~)"
```

---

## Task 15: 在每章末尾追加 `cheat-sheet` block

**Files:**
- Modify: 6 个 mini-review 文件的最后一节，追加 cheat-sheet block：
  - `src/data/lessons/chapter1/modes-movement-mini-review.ts`
  - `src/data/lessons/chapter2/words-mini-review.ts`
  - `src/data/lessons/chapter3/operators-mini-review.ts`
  - `src/data/lessons/chapter4/in-line-precision-review.ts`
  - `src/data/lessons/chapter5/textobjects-mega-review.ts`
  - `src/data/lessons/chapter6/speedrun-challenge.ts`

- [ ] **Step 1: 在每个 mini-review 文件的 contentBlocks 末尾追加**

每个文件追加一项（注意 `chapterId` 参数）：

```typescript
    {
      type: 'cheat-sheet',
      config: { chapterId: 'chapter1' }   // ← 替换为对应章节
    }
```

- [ ] **Step 2: 同步 i18n**

`src/i18n/locales/zh/lessons.json` 与 `zh-lively/lessons.json` 的每个对应 slug 的 `content` 对象中，把最后一个 contentBlock 索引加上（仅 cheat-sheet 不需要文本翻译，因为组件内自动取已有 key-list 的 desc 翻译）。**无需额外 i18n 写入**，因为 cheat-sheet 自身没有翻译字段，但要确保 `content.{N}` 不漏。

实际操作：cheat-sheet block 不渲染 markdown 字符串，所以 `LessonView` 走的是 `content.{N}` lookup —— **但** cheat-sheet block 内组件自己拿翻译。所以 i18n JSON **不用加索引**。验证：手测时检查 console 没有 "returned an object instead of string" 警告。

- [ ] **Step 3: 写 Cheat Sheet UI 文案翻译**

在 `src/i18n/locales/zh/challenge.json` 增加：

```json
"cheatSheet": {
  "download": "下载为 PNG",
  "exporting": "导出中…"
}
```

在 `zh-lively/challenge.json` 增加：

```json
"cheatSheet": {
  "download": "把这张卡片存下来",
  "exporting": "正在生成图片…"
}
```

英文兜底已在 `CheatSheetBlock.tsx` 的 `defaultValue` 写死，无需 `en/challenge.json` 修改。

- [ ] **Step 4: 手测**

```bash
npm run dev
# 依次打开 6 个 mini-review，每个末尾应出现 Cheat Sheet 卡 + 下载按钮
# 点 "Download as PNG" 验证文件可打开且内容齐全
```

- [ ] **Step 5: typecheck + build**

```bash
npx tsc -p tsconfig.app.json --noEmit
npm run build 2>&1 | tail -10
```

Expected: PASS；bundle 增量 < 50 KB。

- [ ] **Step 6: Commit**

```bash
git add src/data/lessons src/i18n/locales/zh/challenge.json src/i18n/locales/zh-lively/challenge.json
git commit -m "feat(content): add per-chapter cheat-sheet blocks at end of each mini-review"
```

---

## Task 16: i18n 翻译 5 节新课

**Files:**
- Modify: `src/i18n/locales/zh/lessons.json`, `src/i18n/locales/zh-lively/lessons.json`

- [ ] **Step 1: 在 zh/lessons.json 增加 5 节翻译**

为以下 slug 添加完整翻译（结构参照 `motions-line-bounds`）：
- `motions-file-bounds`
- `motions-paragraph`
- `motions-bracket-match`
- `operator-shortcuts`
- `operator-case`

每节包含 `title`、`shortDescription` 与 `content.{0..N}` 各索引（markdown 是 string，key-list/run-example/challenge 是带 `keys`/`tracks`/`steps`/`goals` 的对象）。

> **关键格式**：参考 `motions-line-bounds` 的现有翻译结构，逐索引对齐 contentBlocks 顺序。**禁止用中文全角引号**。

- [ ] **Step 2: zh-lively/lessons.json 同步加入活泼版翻译**

参考 zh-lively/lessons.json 中 `motions-line-bounds` 的活泼版语气。

- [ ] **Step 3: 验证 JSON 与 i18n 结构**

```bash
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/zh/lessons.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/zh-lively/lessons.json','utf8'))"
```

```bash
npm run dev
# 切换语言到 中文 / 中文(活泼)，巡检 5 节新课，console 不应有 i18next 警告
```

- [ ] **Step 4: Commit**

```bash
git add src/i18n/locales/zh/lessons.json src/i18n/locales/zh-lively/lessons.json
git commit -m "feat(i18n): add zh / zh-lively translations for v2.1.0 lessons"
```

---

## Task 17: Neovim 对拍测试

**Files:**
- Modify: `src/core/tests/exhaustiveTestCases.ts`

- [ ] **Step 1: 新增 FeatureId 与测试 case 池**

在 `exhaustiveTestCases.ts` 适当位置增加：

```typescript
// gg / G / {N}G
addCases('motion_gg_G', [
  { keys: 'gg', initial: ['  alpha', 'beta', '  gamma'], cursor: { line: 2, col: 4 } },
  { keys: 'G', initial: ['a', 'b', 'c'], cursor: { line: 0, col: 0 } },
  { keys: '2G', initial: ['a','b','c'], cursor: { line: 0, col: 0 } },
  { keys: '5G', initial: ['a','b','c'], cursor: { line: 0, col: 0 } },  // out of range
]);

// { / }
addCases('motion_brace', [
  { keys: '}', initial: ['a','','b','','c'], cursor: { line: 0, col: 0 } },
  { keys: '{', initial: ['a','','b','','c'], cursor: { line: 4, col: 0 } },
]);

// % single-line
addCases('motion_percent', [
  { keys: '%', initial: ['foo(bar)'], cursor: { line: 0, col: 3 } },
  { keys: '%', initial: ['(a(b)c)'], cursor: { line: 0, col: 0 } },
]);

// Case operators
addCases('operator_case', [
  { keys: '~', initial: ['hello'], cursor: { line: 0, col: 0 } },
  { keys: 'guw', initial: ['HELLO WORLD'], cursor: { line: 0, col: 0 } },
  { keys: 'gU$', initial: ['abc def'], cursor: { line: 0, col: 4 } },
  { keys: 'g~~', initial: ['Hello World'], cursor: { line: 0, col: 0 } },
]);
```

> 实际 `addCases` 函数签名需匹配现有代码；上面是示意。请参考 `src/core/tests/exhaustiveTestCases.ts` 已有 case 的格式做调整。

- [ ] **Step 2: 跑对拍测试**

```bash
npx vitest run --pool=threads src/core/tests/exhaustiveTest.*.test.ts --maxWorkers=8 2>&1 | grep -EA20 "Failed Tests|Test Files"
```

Expected: 新增 case 全 pass（若 Neovim 不可用会 skip，亦视为通过）。

- [ ] **Step 3: Commit**

```bash
git add src/core/tests/exhaustiveTestCases.ts
git commit -m "test(core): add Neovim parity cases for gg/G/brace/percent/case-op"
```

---

## Task 18: 文档与版本号更新

**Files:**
- Modify: `src/version.ts`, `package.json`, `README.md`, `CLAUDE.md`

- [ ] **Step 1: 版本号**

`src/version.ts`：

```typescript
export const REL_VERSION = '2.1.0';
export const DEV_VERSION = '2.1.0';
```

`package.json`：

```json
"version": "2.1.0",
```

- [ ] **Step 2: README CHANGELOG**

在 README.md 顶部 CHANGELOG 节追加（不超过 4 条）：

```markdown
### v2.1.0
- 新增基础导航：gg / G / {N}G / { / } / %
- 新增大写快捷：D / C / Y / S 与大小写操作符 ~ / gu / gU / g~
- Chapter 1 / 3 共 5 节新课程
- 每章末尾追加可下载的 Cheat Sheet
```

- [ ] **Step 3: CLAUDE.md 更新命令清单**

在 `## Vim Engine Capabilities` 的"已支持的命令"段落中追加：

- 文件导航：`gg`、`G`、`{N}G`
- 段落跳转：`{`、`}`
- 括号匹配：`%`（单行）
- 大写快捷：`D`、`C`、`Y`、`S`
- 大小写：`~`、`gu{motion}`、`gU{motion}`、`g~{motion}`

并在"尚未支持"区移除（若有）相关条目。

- [ ] **Step 4: 全量验证**

```bash
npx tsc -p tsconfig.app.json --noEmit
npm run lint 2>&1 | tail -3
npm run build 2>&1 | tail -5
bash utils/vitest-quickcheck.sh
```

Expected: 全 PASS；lint 错误数不超过 master baseline (18)。

- [ ] **Step 5: Commit**

```bash
git add src/version.ts package.json package-lock.json README.md CLAUDE.md
git commit -m "chore: bump to v2.1.0, update CHANGELOG + command inventory"
```

- [ ] **Step 6: Tag**

```bash
git tag v2.1.0
git push origin master --tags
```

> 注：push 需用户授权（与现有 `/ccg:commit` 规约一致），不要在未确认时自动 push。

---

## Self-Review

**1. Spec coverage:**
- ✅ gg / G / {N}G → Task 2-3
- ✅ { / } → Task 4
- ✅ % → Task 5
- ✅ D / C / Y / S → Task 6
- ✅ ~ / gu / gU / g~ → Task 7-8
- ✅ Cheat Sheet block + 6 mini-review 嵌入 → Task 9 + 15
- ✅ 5 节新课 → Task 10-14
- ✅ i18n zh + zh-lively → Task 16
- ✅ 对拍测试 → Task 17
- ✅ 文档 + 版本号 → Task 18
- ✅ 明确**不做** H/M/L 与 Ctrl-d/u/f/b（roadmap 已记录推迟到 v2.4）

**2. Placeholder scan:**
- ✅ 没有 TBD / TODO
- ✅ 所有引擎代码块都给了完整实现
- ✅ 所有测试 case 都写出真实代码
- ⚠️ Task 16 i18n 翻译没有给完整 5 节中文文本（写"参照现有结构逐索引对齐"）— 这是合理的，因为翻译文本应由人工/翻译工具产出，plan 里只给规则即可
- ⚠️ Task 17 `addCases` 函数签名标注"示意，请参考已有"— 是因 plan 写作时未读 exhaustiveTestCases.ts 内部，留给执行者按现有模式跟随

**3. Type consistency:**
- ✅ `Motion` 联合：gg / G / { / } / % 在 Task 1 引入，Task 2/4/5 使用
- ✅ `Operator` 联合：'gu' / 'gU' / 'g~' 在 Task 1 引入，Task 8 使用
- ✅ `pendingG` 字段：Task 3 引入并 init，Task 8 复用
- ✅ `CheatSheetConfig`：Task 1 定义，Task 9 消费，Task 15 使用
- ✅ 函数名 `gotoFirstLine` / `gotoLastLine` / `gotoLineN` / `paraNext` / `paraPrev` / `bracketMatch` 在 Task 2/4/5 一致
- ✅ `applyCaseRange` / `toggleCharCase` / `CaseMode` 在 Task 7-8 一致

---

## 风险与回退方案

| 风险 | 触发条件 | 回退 |
|---|---|---|
| g pending 影响现有 dot-command 测试 | `bash utils/vitest-quickcheck.sh` 失败数增加 | revert Task 3 commit，重新设计 pendingG 的范围（只在 normal 模式生效，不进 changeRecording） |
| `%` 多行用例触发 false positive | 用户报告 cursor 跳错 | 在 lesson 文案加 "单行限制" 提示（已写在 Task 12） |
| html-to-image 浏览器兼容性 | 老 Safari 报错 | toPng catch + fallback 到打印对话框 |
| 章节中部插课影响进度记录 | 用户进度 localStorage 中 slug 不变 | **无影响**：旧 slug 仍存在，新 slug 是 fresh entry |

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-19-v2.1.0-foundation-supplement.md`. Two execution options:

**1. Subagent-Driven (recommended)** — 每个 Task 派发独立子 agent，期间评审、快速迭代

**2. Inline Execution** — 在当前会话用 `superpowers:executing-plans` 批量执行，含 checkpoint

**Which approach?**
