# v2.8 Daily Workflow Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polish Vimprove’s daily-use experience with an accurate support matrix, visible learning paths, small Ex batch-edit upgrades, and an Operator Gym minigame.

**Architecture:** Keep all changes within existing boundaries: Settings owns the support matrix, HomePage owns top-level route cards, `exCommands.ts` owns the narrow Ex parser, and minigames are rendered through `LessonView` content blocks.

**Tech Stack:** React 19, TypeScript, Vite, Vitest, i18next, Tailwind 3, existing pure Vim reducer.

---

## Files

- Modify: `src/components/settings/VimStatusTab.tsx`.
- Modify: `src/i18n/locales/{en,zh,zh-lively}/settings.json`.
- Modify: `src/pages/HomePage.tsx` and `src/App.tsx`.
- Modify: `src/i18n/locales/{en,zh,zh-lively}/home.json`.
- Modify: `src/core/exCommands.ts` and `src/core/exCommands.test.ts`.
- Modify: `src/core/types.ts` for `OperatorGymGameConfig` and `operator-gym` content block.
- Create: `src/components/minigame/OperatorGymGame.tsx`.
- Modify: `src/components/lesson/LessonView.tsx`.
- Modify: one review lesson to include Operator Gym, preferably `src/data/lessons/chapter5/textobjects-mega-review.ts`.
- Modify: `src/i18n/translation-structure.test.ts` and `src/data/lesson-registry.test.ts`.
- Modify: `README.md`, `CLAUDE.md`, `src/version.ts`, `package.json`, `package-lock.json`.

---

## Tasks

- [x] **Task 1: Fix Vim Status matrix**
  - Update command categories and statuses to match current v2.7 support.
  - Add missing category translation keys.
  - Run typecheck and lint.

- [x] **Task 2: Add Home learning paths**
  - Add three route cards to HomePage.
  - Add `onLearningPath(slug)` callback in App.
  - Add home translations.
  - Run typecheck and lint.

- [x] **Task 3: Extend Ex command tests**
  - Add failing tests for `1,2s/foo/bar/g` and `g/debug/s/foo/bar/g`.
  - Run focused test and confirm failure before implementation.

- [x] **Task 4: Implement narrow Ex range/global commands**
  - Extend substitute parser to support current line, whole buffer, numeric line ranges, and global line filtering.
  - Keep replacement literal.
  - Run focused Ex tests.

- [x] **Task 5: Add Operator Gym**
  - Add config and content block type.
  - Implement minigame component.
  - Render it in LessonView.
  - Add it to the Chapter 5 review lesson.
  - Run lesson registry and translation structure tests.

- [x] **Task 6: Version, docs, and verification**
  - Bump to `2.8.0`.
  - Update README and CLAUDE.
  - Run final verification: `npm run typecheck`, `npm run lint`, focused tests, `bash utils/vitest-quickcheck.sh`, `npm run build`.

---

## Verification Commands

```bash
npx vitest run --pool=threads src/core/exCommands.test.ts src/data/lesson-registry.test.ts src/i18n/translation-completeness.test.ts src/i18n/translation-structure.test.ts
npm run typecheck
npm run lint
bash utils/vitest-quickcheck.sh
npm run build
```
