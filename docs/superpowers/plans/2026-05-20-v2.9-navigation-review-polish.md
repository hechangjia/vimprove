# v2.9 Navigation Review Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add screen-level navigation commands, Chapter 13 lessons, and a Scroll Surfer minigame.

**Architecture:** Extend `VimState` with deterministic viewport metadata. Implement screen navigation in `vimReducer.ts`, then expose it through challenges and a new minigame content block.

**Tech Stack:** React 19, TypeScript, Vite, Vitest, i18next, Tailwind 3, existing pure Vim reducer.

---

## Files

- Modify: `src/core/types.ts` for viewport fields and `ScrollSurferGameConfig`.
- Modify: `src/core/stateUtils.ts` for snapshots and pending-state clearing.
- Modify: `src/core/vimReducer.ts` for `Ctrl-d/u/f/b`, `zz/zt/zb`, and `H/M/L`.
- Create: `src/core/screenNavigation.test.ts`.
- Create: `src/components/minigame/ScrollSurferGame.tsx`.
- Modify: `src/components/lesson/LessonView.tsx`.
- Create: `src/data/lessons/chapter13/{screen-scroll-basics,viewport-positioning,screen-line-jumps,screen-navigation-review}.ts`.
- Modify: `src/data/categories.ts`, `src/data/index.ts`, `src/data/lesson-registry.test.ts`.
- Modify: `src/i18n/locales/{zh,zh-lively}/lessons.json` and `src/i18n/locales/{en,zh,zh-lively}/challenge.json`.
- Modify: `README.md`, `CLAUDE.md`, `src/data/CLAUDE.md`, `src/version.ts`, `package.json`, `package-lock.json`.

---

## Tasks

- [x] **Task 1: Add screen navigation tests**
  - Add tests for half/full page movement, viewport placement, screen line jumps, and Escape clearing `pendingZ`.
  - Run focused tests and confirm failure before implementation.

- [x] **Task 2: Implement viewport state**
  - Add `viewportTop`, `viewportHeight`, and `pendingZ`.
  - Update snapshots, clear-pending state, and initial state.

- [x] **Task 3: Implement screen navigation commands**
  - Implement `Ctrl-d/u/f/b`, `z` pending commands, and `H/M/L`.
  - Run focused tests until green.

- [x] **Task 4: Add Scroll Surfer**
  - Add config and content block type.
  - Implement minigame component.
  - Render it in LessonView.
  - Run typecheck and lint.

- [x] **Task 5: Add Chapter 13 lessons**
  - Add four lessons.
  - Register `chapter13` and update lesson count from 61 to 65.
  - Add zh and zh-lively translations.
  - Run lesson registry and translation tests.

- [x] **Task 6: Version, docs, and verification**
  - Bump to `2.9.0`.
  - Update README and CLAUDE docs.
  - Run final verification: `npm run typecheck`, `npm run lint`, focused tests, `bash utils/vitest-quickcheck.sh`, `npm run build`.

---

## Verification Commands

```bash
npx vitest run --pool=threads src/core/screenNavigation.test.ts src/data/lesson-registry.test.ts src/i18n/translation-completeness.test.ts src/i18n/translation-structure.test.ts
npm run typecheck
npm run lint
bash utils/vitest-quickcheck.sh
npm run build
```
